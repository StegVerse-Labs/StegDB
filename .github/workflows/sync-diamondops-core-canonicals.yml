name: sync-diamondops-core-canonicals

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "If true, do not push/PR (prints what would change)."
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest

    env:
      # PAT required to push branches + open PRs in OTHER repos.
      # Must be a classic PAT or fine-grained PAT with:
      # - Contents: Read/Write on target repos
      # - Pull requests: Read/Write on target repos
      GH_TOKEN: ${{ secrets.STEGVERSE_BOT_TOKEN }}

      # Canonical source
      CANON_SOURCE_REPO: "stegverse-labs/diamondops-core"
      CANON_SOURCE_REF: "main"
      CANON_REGISTRY: "registry/diamondops_core_canonical_docs.json"

      # Target repos
      TARGETS: |
        stegverse-labs/hydrasafe
        stegverse-labs/reactorops
        stegverse-labs/yieldos

    steps:
      - name: Checkout StegDB
        uses: actions/checkout@v4

      - name: Preflight - require bot token
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${GH_TOKEN:-}" ]]; then
            echo "::error::Missing secret STEGVERSE_BOT_TOKEN (PAT required to push/PR into other repos)."
            exit 2
          fi
          echo "Bot token present."

      - name: Checkout DiamondOps-Core (canonical source)
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/diamondops-core
          git clone --depth 1 --branch "${CANON_SOURCE_REF}" "https://github.com/${CANON_SOURCE_REPO}.git" /tmp/diamondops-core
          test -f /tmp/diamondops-core/docs/POSITION.md
          test -f /tmp/diamondops-core/docs/LIABILITY_BOUNDARY.md
          echo "Canonical source checkout OK."

      - name: Sync canonicals into target repos + open PRs
        shell: bash
        run: |
          set -euo pipefail

          STEGDB_ROOT="${GITHUB_WORKSPACE}"
          DRY="${{ inputs.dry_run }}"

          # Use a stable non-repo CWD so we never delete our current directory.
          cd /

          while IFS= read -r full; do
            full="$(echo "$full" | xargs)"
            [[ -z "$full" ]] && continue

            name="$(basename "$full")"
            echo "----"
            echo "Target: $full"

            work="/tmp/target-${name}"
            rm -rf "$work"
            mkdir -p "$work"

            # Clone target repo with PAT (must have RW + PR perms for targets)
            git clone "https://x-access-token:${GH_TOKEN}@github.com/${full}.git" "$work"

            # Run sync (NOTE: script path is absolute to StegDB workspace)
            python "${STEGDB_ROOT}/tools/sync_canonical_docs.py" \
              --registry "${STEGDB_ROOT}/${CANON_REGISTRY}" \
              --source-root /tmp/diamondops-core \
              --target-repo-root "$work" \
              --stegdb-root "${STEGDB_ROOT}" \
              --repo-name "$name" \
              $( [[ "$DRY" == "true" ]] && echo "--dry-run" || true )

            if [[ "$DRY" == "true" ]]; then
              echo "Dry-run for $full completed."
              continue
            fi

            # No changes? move on.
            if git -C "$work" diff --quiet; then
              echo "No changes for $full."
              continue
            fi

            BR="canonicals/diamondops-core-$(date -u +%Y%m%d%H%M%S)"

            git -C "$work" config user.name "stegverse-bot"
            git -C "$work" config user.email "stegverse-bot@users.noreply.github.com"

            git -C "$work" checkout -b "$BR"
            git -C "$work" add -A
            git -C "$work" commit -m "chore(docs): sync DiamondOps-Core canonical docs"
            git -C "$work" push -u origin "$BR"

            TITLE="chore(docs): sync DiamondOps-Core canonicals"
            BODY=$'This PR was generated by StegDB.\n\n- Canonical source: https://github.com/'"${CANON_SOURCE_REPO}"$'\n- Registry: `'"${CANON_REGISTRY}"$'`\n\nDo not edit these files locally—update DiamondOps-Core canonicals and re-run.'

            # Create PR (gh uses GH_TOKEN from env)
            gh pr create \
              --repo "$full" \
              --head "$BR" \
              --base "main" \
              --title "$TITLE" \
              --body "$BODY" || {
                echo "::warning::PR creation failed (maybe already exists). Branch: $BR"
              }

          done <<< "${TARGETS}"
